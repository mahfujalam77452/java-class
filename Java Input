public class ArrayTest {
    public static void main(String[] args) throws java.io.IOException{
      char c;
      c=(char)System.in.read();
      System.out.println(c);
      //Show if we take only one input whithout skip function then what will have after clicking inter.
      //Firstly take input ab then a b and then a.
      System.in.skip(100);
      //to skip new line for avoid taking new line as input.
      //here to skip char we can read a new character like (char)System.in.read( )
      c=(char)System.in.read();
      System.out.println(c);

     
    }
}
